<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- ScrollView 내부에는 단 하나의 컨테이너(LinearLayout) -->
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- 자녀 위치 정보 영역 -->
        <TextView
            android:id="@+id/tv_location_info"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="위치 찾기"
            android:textSize="16sp"
            android:padding="8dp"/>

        <Button
            android:id="@+id/btn_open_map"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="지도 열기"
            android:enabled="true" />

        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:indeterminate="true"/>

        <TextView
            android:id="@+id/tv_error_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FF0000"
            android:padding="8dp"
            android:visibility="gone"/>


        <!-- 시간 설정 섹션 -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="시간 설정"
            android:textStyle="bold"
            android:textSize="16sp"
            android:paddingVertical="8dp"
            android:paddingTop="16dp"/>

        <!-- 시간 설정 버튼 -->
        <Button
            android:id="@+id/btn_set_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="시간 설정" />

        <!-- 선택된 시간을 표시할 TextView -->
        <TextView
            android:id="@+id/tv_selected_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="선택된 시간: 없음"
            android:textSize="16sp"
            android:padding="8dp"/>

        <!-- Account Info Button -->
        <Button
            android:id="@+id/btn_account_info"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="계정 정보" />

        <!-- Account Connect Button -->
        <Button
            android:id="@+id/btn_account_connect"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="계정 연결"
            android:layout_marginTop="16dp" />

        <!-- TextView to Display Account Info -->
        <TextView
            android:id="@+id/tv_account_info"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="계정 정보가 표시됩니다."
            android:textSize="16sp"
            android:layout_marginTop="24dp" />

        <!-- TextView to Display Partner ID -->
        <TextView
            android:id="@+id/tv_partner_id"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="파트너 ID가 표시됩니다."
            android:textSize="16sp"
            android:layout_marginTop="16dp" />
    </LinearLayout>


</ScrollView>
